package main.kotlin.weekend.classes

/**
 * When defining a class with no arguments in the primary constructor,
 * we can omit the primary constructor altogether
 * class Person constructor() is reduced to class Person
 */
class Person